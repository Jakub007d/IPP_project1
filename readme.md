Parser of fictional programing language that parses it to xml format.
Coded using PHP.


USAGE:

1. parse.php <  input (typ IPPcode22) , output will be shown on STDOUT
2. parse.php <  input (typ IPPcode22)  > output (typ xml)
